<div class="container w-full bg-gray-400 flex items-center">
  <header class="h-[40px] w-full flex flex-row items-center justify-between">
    <ul class="flex flex-row items-center gap-4">
      <li>Instagram</li>
      <li>Facebook</li>
      <li>Twitter</li>
    </ul>
    <ul class="flex flex-row items-center gap-2">
      <li class="inline-block px-4 py-2">Home</li>
      <li class="inline-block px-4 py-2">About</li>
      <li class="inline-block px-4 py-2">Contact</li>
    </ul>
  </header>
  <nav></nav>
</div>

{% schema %}
  {
    "name": "Navigation",
    "settings": [],
    "blocks": [
      {
        "type": "header",
        "name": "Header",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Header"
          }
        ]
      },
      {
        "type": "nav",
        "name": "Nav",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Nav"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Navigation",
        "category": "Navigation"
      }
    ]
  }
{% endschema %}